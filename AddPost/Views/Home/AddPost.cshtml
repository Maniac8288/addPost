@model AddPost.Models.Post
@using AddPost.Models;
@{
    ViewBag.Title = "Добавление поста";
}

<h2 class="text-center">Добавление поста</h2>
<hr />
<div class="container ">
    <form class="form-horizontal  col-md-12 " name="Post" method="post" enctype="multipart/form-data">
        <div class="form-group  ">
            <label for="NamePost" class="col-sm-2 control-label">Название</label>

            <div class="col-md-10" >
                <input type="text" class="form-control" id="NamePost" name="NamePost" required placeholder="Заголовок" />
            </div>
        </div>
        <div class="form-group">
            <label for="Category" class="col-sm-2 control-label">Категория</label>
            <div class="col-md-10">
                <select class="form-control" required name="selectedCategory">
                    @foreach(string element in Post.Category)
                    {
                        <option>@element</option>
                    }
                </select>



                <div class="btn-group pull-right">
                    <button type="button" data-toggle="dropdown" class="btn btn-default dropdown-toggle ">Выберите категорию <span class="caret"></span></button>
                    <ul class="dropdown-menu">
                        @foreach (var item in Post.News)
                        {
                           <li><a href="#">@item</a></li>
                        }

                    </ul>
                </div>
                <div class="dropdown">
                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
                        Выпадающий список
                    </button>
                    <ul class="dropdown-menu">
                        @foreach (string element in Post.Category)
                        {
                            <li class="dropdown-header">@element</li> foreach (var item in Post.News)
                            {
                        <li><a href="#">@item</a></li>}
                        }
                       
                    </ul>
                </div>

            </div>

        </div>

        <div class="form-group">
            <div class="col-sm-offset-2 col-md-12">
                <textarea class="form-control col-lg-50" cols="150" rows="25"  id="contentPost" required name="contentPost" ></textarea>
            </div>
        </div>
        <hr />
        <div class="form-group col-xs-12 ">
            <label for="Tags" class="col-sm-2 control-label">Тэги</label>
            <div class="col-xs-10">
                <textarea cols="150" rows="3" class="form-control" id="Tags" name="Tags" required placeholder="Введите тэги через запятую"></textarea>
            </div>
           
        </div>
        <div class="form-group ">
            <label for="dateAddPost" class="col-sm-2 control-label" >Дата создания</label>
            <div class="col-md-2">
                <input type="date" class="form-control" required name="dateAddPost" id="dateAddPost" />
                </div> 
          
                <label for="File" class="col-sm-2 control-label">Прикрепить файл</label>
            <div class="col-xs-4">
                <input type="file" class="form-control " name="upload" id="upload" />
            </div>
            <input type="submit" class="btn btn-defaultt  " name="save" />
            </div>
           
            
    </form>
    </div>
    <hr />
    
